const std = @import("std");

pub fn main() !void{
	//std.debug.panic("Whoah!", .{});
	//Feature rich way of panicing manually in runtime.

	//Simpler, less featured way of panicing manually in runtime:
	@panic("Hehehehehe");
}